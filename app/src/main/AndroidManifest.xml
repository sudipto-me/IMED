<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.imed">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MenuBar"
            android:label="@string/title_activity_menu_bar"
            android:theme="@style/AppTheme" />
        <activity android:name=".AboutImed.AboutImed" />
        <activity android:name=".SebaProdan.SebaProdan" />
        <activity android:name=".Officials.ListofOfficers" />
        <activity android:name=".Officials.PersonalsList" />
        <activity android:name=".Officials.OfficialsViewActivity"></activity>
    </application>

</manifest>